#include <ros/ros.h>
#include "see_localization/SeeLocalization.hpp"

/*int main(int argc, char** argv)
{
  ros::init(argc, argv, "see_localization");
  ros::NodeHandle nodeHandle("~");

  //SeeLocalization rosPackageTemplate(nodeHandle);
  SeeLocalization SeeLocalization

  // @param double frequency [Hz] in which to repeat the loop
  ros::Rate rate(1);
  while(ros::ok())
  {
    rosPackageTemplate.run();
    rate.sleep();
  }
  return 0;
}*/
